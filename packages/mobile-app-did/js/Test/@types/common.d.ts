/**
 * @author hzz780
 * @description Add common or key type and configuration here.
 */

/* Constrain the storage keys to avoid unnecessary storage */
export interface ILocalStorage {
  customLanguageCode: string;
  wallet: any;
  test: string;
}
