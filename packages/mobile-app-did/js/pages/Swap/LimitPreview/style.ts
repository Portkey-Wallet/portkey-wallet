import { makeStyles } from '@rneui/themed';
import { pTd } from 'utils/unit';

export const getStyles = makeStyles(() => ({
  previewAmountCard: {
    marginTop: pTd(8),
  },
  infoRowContainer: {
    marginTop: pTd(8),
  },
  promptCard: {
    marginTop: pTd(16),
  },
  footerWrap: {
    marginTop: pTd(16),
  },
}));
